info:
  name: set-due
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:3000/api/report/set-due/1
  body:
    type: json
    data: |-
      {
        "dueDate": "2027-09-17"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
