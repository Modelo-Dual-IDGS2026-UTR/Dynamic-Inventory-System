info:
  name: set-status
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:3000/api/report/set-status/1
  body:
    type: json
    data: |-
      {
        "status": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
