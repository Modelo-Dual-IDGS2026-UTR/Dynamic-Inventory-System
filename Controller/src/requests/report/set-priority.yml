info:
  name: set-priority
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:3000/api/report/set-priority/1
  body:
    type: json
    data: |-
      {
        "priority": "3"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
