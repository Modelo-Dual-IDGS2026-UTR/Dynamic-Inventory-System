info:
  name: delete-user
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:3000/api/user/delete-user/4
  body:
    type: json
    data: |-
      {
        "fk_role": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
