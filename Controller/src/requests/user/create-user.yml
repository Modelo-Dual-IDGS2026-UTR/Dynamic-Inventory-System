info:
  name: create-user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/user/create-user
  body:
    type: json
    data: |-
      {
        "firstName": "Ron",
        "lastName": "Guapo",
        "email": "ronguapo@gmail.com",
        "area": "IT",
        "googleUserId": "valesverga"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
