info:
  name: promotion
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:3000/api/user/promotion/1
  body:
    type: json
    data: |-
      {
        "fk_role": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
