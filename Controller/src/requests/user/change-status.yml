info:
  name: change-status
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/api/user/change-status/1
  body:
    type: json
    data: |-
      {
        "userStatus": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
